export const quickStart = `# Quick Start

Readmade runs entirely in the browser. You can use the hosted app or run it from source.

## Hosted app

Open [readmade.vercel.app](https://readmade.vercel.app) and start editing. Nothing to install.

## Run from source

You need a current Node.js LTS release and npm.

\`\`\`bash
git clone https://github.com/bilalmlkdev/readmade.git
cd readmade
npm install
npm run dev
\`\`\`

Vite prints the local URL after the development server starts. Open it in your browser.

## Production build

\`\`\`bash
npm run build
npm run preview
\`\`\`

\`npm run build\` writes optimized files to \`dist/\`. \`npm run preview\` serves that build locally.

## Lint

Before you open a pull request, both checks must pass:

\`\`\`bash
npm run lint
npm run build
\`\`\`

## Your first README

1. Open the [editor](/app)
2. Enter an email to create a local workspace (no verification)
3. Start from a [template](/docs/templates) or an empty canvas
4. Add blocks from the left palette
5. Click a block to edit its content
6. Drag handles to reorder sections
7. Check the live preview on the right
8. Switch to the Code tab to inspect raw Markdown
9. Download \`README.md\` or copy it to the clipboard

## Tips for day one

- Keep the title block first so the project name is always on top
- Put Installation and Usage next - that is what readers try first
- Save often with <kbd>Ctrl</kbd>/<kbd>Cmd</kbd> + <kbd>S</kbd> to download a snapshot
- Open history to restore a previous version if you experiment too hard

## Next steps

- Learn the [editor layout](/docs/editor)
- Browse [block types](/docs/blocks)
- Review [keyboard shortcuts](/docs/shortcuts)
`;
