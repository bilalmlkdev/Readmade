export const shortcuts = `# Keyboard Shortcuts

Shortcuts work across the editor. On Mac the modifier is <kbd>Cmd</kbd>; on Windows and Linux it is <kbd>Ctrl</kbd>.

## Global

| Shortcut | Action |
| --- | --- |
| <kbd>Ctrl</kbd>/<kbd>Cmd</kbd> + <kbd>S</kbd> | Download the README |
| <kbd>Ctrl</kbd>/<kbd>Cmd</kbd> + <kbd>Shift</kbd> + <kbd>P</kbd> | Open the block palette |
| <kbd>Ctrl</kbd>/<kbd>Cmd</kbd> + <kbd>Shift</kbd> + <kbd>R</kbd> | Reset the document |
| <kbd>Ctrl</kbd>/<kbd>Cmd</kbd> + <kbd>Shift</kbd> + <kbd>C</kbd> | Copy Markdown to the clipboard |

## Focus behavior

When focus is inside an input, textarea, or contenteditable field, plain keys type as usual. Modifier shortcuts above still work so you can save or copy without leaving the field.

## Mouse and touch

- Click a block header to expand or collapse it
- Drag the handle to reorder
- Click palette icons to insert blocks
- Click preview tabs to switch Preview and Code

## Tips

- Use <kbd>Ctrl</kbd> + <kbd>S</kbd> as a habit after larger edits
- Palette shortcut is fastest when the sidebar is minimized
- Reset always asks for confirmation - the shortcut will not wipe work silently

## Related

- [Editor guide](/docs/editor)
- [Preview and export](/docs/preview-export)
`;
