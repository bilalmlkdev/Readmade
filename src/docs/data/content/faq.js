export const faq = `# FAQ

## Do I need an account?

No. Open the editor with any email address for a local display name. Nothing is verified or stored on a server.

## Where is my data?

In your browser's \`localStorage\`, under the \`readmade\` keys. See [Storage and privacy](/docs/storage).

## Does Readmade upload screenshots?

No. You provide an HTTPS image URL; the exported Markdown keeps that URL.

## Which export format should I use?

Markdown \`.md\` for the repository README. Use HTML only when you need a standalone rendered snapshot.

## Why does my preview differ from GitHub?

The preview aims for GitHub-like rendering. Edge cases (custom themes, unusual embeds) can differ slightly. Always glance at the Code tab before committing.

## Can I use it offline?

Yes. After the app is loaded, editing and export work without a network connection. First load and hosted assets need the network.

## How do I reset the document?

Use the reset action in the header (or <kbd>Ctrl</kbd>/<kbd>Cmd</kbd> + <kbd>Shift</kbd> + <kbd>R</kbd>). A confirmation dialog protects against accidental clears.

## How many history snapshots are kept?

Up to 8. Older entries drop off as new autosaves arrive.

## Is there a free tier?

The editor is free for local use. See [pricing on the landing page](/#pricing) if you are looking at hosted plans.

## How do I report a bug?

Open a GitHub issue with steps to reproduce. Security issues go through the [security policy](/docs/security) instead.

## How do I contribute?

Read [Contributing](/docs/contributing), run lint and build, then open a focused pull request.

## Related

- [Introduction](/docs/intro)
- [Quick Start](/docs/quick-start)
- [Block reference](/docs/blocks)
`;
